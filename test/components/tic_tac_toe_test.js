// Removed temporary 

/*
import { renderComponent , expect } from '../test_helper';
import TicTacToe from '../../src/components/tic_tac_toe';

describe ('TicTacToe', () => {
    let component;

    beforeEach(() => {
        component = renderComponent(TicTacToe);
    });

    it('renders TicTacToe', () => {
        expect(component).to.exist;
    });
});
*/