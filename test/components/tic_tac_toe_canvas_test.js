// Removed temporary 

/*
import { renderComponent , expect } from '../test_helper';
import Canvas from '../../src/components/tic_tac_toe_canvas';

describe ('TicTacToeCanvas', () => {
    let component;

    beforeEach(() => {
        component = renderComponent(Canvas);
    });

    it('renders canvas', () => {
        expect(component).to.exist;
    });
});
*/